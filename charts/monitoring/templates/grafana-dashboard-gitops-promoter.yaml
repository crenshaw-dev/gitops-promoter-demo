apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-gitops-promoter
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
    app.kubernetes.io/part-of: monitoring
data:
  gitops-promoter.json: |-
{{ .Files.Get "dashboards/gitops-promoter.json" | indent 4 }}
