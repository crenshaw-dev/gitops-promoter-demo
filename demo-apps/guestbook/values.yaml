replicaCount: 1

# Bumped by manifests/demo-churn CronJob; surfaces in pod annotations for every guestbook environment.
demoChurn:
  lastBumped: "2026-04-15T06:00:10Z"

image:
  repository: gcr.io/google-samples/gb-frontend
  tag: v5
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
